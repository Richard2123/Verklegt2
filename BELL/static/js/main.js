
let loadFileOne = function(event) {
let image = document.getElementById('output-one');
image.src = URL.createObjectURL(event.target.files[0]);
};

let loadFileTwo = function(event) {
let image = document.getElementById('output-two');
image.src = URL.createObjectURL(event.target.files[0]);
};

let loadFileThree = function(event) {
let image = document.getElementById('output-three');
image.src = URL.createObjectURL(event.target.files[0]);
};

let loadFileFour = function(event) {
let image = document.getElementById('output-four');
image.src = URL.createObjectURL(event.target.files[0]);
};

let loadFileFive = function(event) {
let image = document.getElementById('output-five');
image.src = URL.createObjectURL(event.target.files[0]);
};

let loadFileProfile = function(event) {
let image = document.getElementById('output-profile');
image.src = URL.createObjectURL(event.target.files[0]);
};


